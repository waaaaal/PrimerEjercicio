
import { BrowserRouter, Route, Routes } from 'react-router-dom';
export function TaskDone() :JSX.Element{
    return (
        <BrowserRouter>
        <div> d</div>
        </BrowserRouter>
    );
  }
  
